# Terraform Utils Docker Image

Designed for use in environments that don't have internet access.  The required providers are installed into the Docker image.

Currently only the AWS provider is included.
